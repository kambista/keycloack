services:
  keycloak:
    image: us-central1-docker.pkg.dev/development-k8s-2021/keycloak/keycloak-custom:latest
    container_name: keycloak
    restart: always
    env_file:
      - .env
    environment:
      - KC_HOSTNAME=mercury-xqa.kambista.com
      - KC_HOSTNAME_STRICT=false
      - KC_HTTP_ENABLED=true
      - KC_PROXY_HEADERS=xforwarded    
    command: start
    expose:
      - "8080"
    networks:
      - keycloak_net

  nginx:
    image: nginx:1.27-alpine
    container_name: nginx-keycloak
    restart: always
    depends_on:
      - keycloak
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/keycloak.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certbot/www:/var/www/certbot:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    networks:
      - keycloak_net

networks:
  keycloak_net: